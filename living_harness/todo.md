# План развития и исследований (TODO)

- [x] 1. **Разделение контекста на системный промпт, память и окно рассуждений**
  - [x] Реализовать структуру контекста (система, память с метками времени, плавающее окно).
  - [x] Добавить логику усечения: при заполнении плавающего окна до 95%, усекать ранние рассуждения до 10% лимита.
  - [x] Добавить модуль `memory/context_manager.py` для управления зонами (см. код: `living_harness/memory/context_manager.py`).

- [x] 2. **Анализ энтропии распределения внимания и зацикливания**
  - [x] Измерить деградацию энтропии в сгенерированных цепочках для выявления алгоритмического срыва.
  - [x] Создать скрипт `analytics/entropy_analyzer.py` для выявления циклов.

- [x] 3. **Реализация функций взаимодействия (tool calling)**
  - [x] Реализовать функцию `ask_user`.
  - [x] Реализовать функцию `new_memory`.
  - [x] Реализовать функцию `overwrite_memory`.
  - [x] Внедрить базовые инструменты в `core/tools.py` (реализовано в `living_harness/core/tools.py`).

- [ ] 4. **Тестирование ультракомпактной модели с размышлениями**
  - [ ] Настроить запуск "бесконечного" потока на модели класса 0.5B (Qwen2.5-0.5B или SmolLM2-360M).
  - [ ] Оценить время удержания осмысленного `<think>` процесса без повторов.
  - [ ] Создать и запустить `experiments/compact_think_loop.py`.

- [ ] 5. **Реализация "Морфологического некро-вычисления" (Архитектуры увядания)**
  - [ ] Разработать математический штраф/шум за доступ к старым данным (имитация тепловой деградации).
  - [ ] Добавить логику затухания релевантности в `core/decay_mechanisms.py`.

- [ ] 6. **Анализ иллюзии внутреннего монолога (Эргономика интерфейса)**
  - [ ] Оценить восприятие "шума" из непрерывной генерации человеком.
  - [ ] Написать черновик научной статьи `articles/illusion_of_monologue.md`.

- [ ] 7. **Анализ энергозатрат непрерывного вычисления (Проблема Стены Памяти)**
  - [ ] Оценить вычислительные затраты на поддержание цикла генерации в архитектуре фон Неймана.
  - [ ] Написать черновик научной статьи `articles/memory_wall_limits.md`.
