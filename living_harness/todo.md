# План развития и исследований (TODO) - Этап 15: Возврат к оригинальной архитектуре Living Harness

- [x] 1. **Анализ результатов предыдущих экспериментов**
  - Изучить логи из `living_harness/results/` с помощью скрипта, чтобы определить среднюю длину записей (параметр `t` для расчета ячеек памяти) и выявить проблемы бесконечного потока.
- [x] 2. **Прототип перехвата и подмены токенов (Эксперимент 1)**
  - Создать скрипт на базе `infinity.py`. Внедрить принудительную подмену: `<eos>` на `<think>` для входа в рассуждения, и `</think>`/`<eos>` на `кстати` для предотвращения остановки.
- [x] 3. **Управление контекстом и метками времени (Эксперимент 2)**
  - Реализовать разделение окна: системный промпт, память с метками времени, область системного времени (рождения/текущее), и плавающее окно рассуждений с усечением (95% -> 10%).
- [x] 4. **Интеграция инструментов управления памятью и диалогом**
  - Разработать функции `new_memory`, `overwrite_memory`, `get_memory_space` (по формуле `(N / 2 - n1 - h) / t`) и `ask_user` для инициации диалога из режима рассуждений.
- [x] 5. **Реализация приложения Living Harness (Итог)**
  - Написать `living_harness/main.py`, поднимающий FastAPI сервис с WebSocket-чатом, интегрирующий разработанный harness для полноценного взаимодействия.
